#!/bin/bash

rm -f $0 # for security issue

### load environment
[ -z "$RUNDIR" ] && RUNDIR=$HOME
source $RUNDIR/vplmodel/toolkit.sh
LOADENV
CHECKENV
EXPORTENV

### run exo
BASEDIR="$RUNDIR/GIT/$EXO"
[ ! -f $BASEDIR/run.sh ] && echo "⚠ File \"run.sh\" missing!" && exit 0
cd $RUNDIR && ./GIT/$EXO/run.sh

# EOF
